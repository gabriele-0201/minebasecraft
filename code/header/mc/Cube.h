#ifndef CUBE_H
#define CUBE_H

namespace cube {

    /**
     * Given the center return the 8 vertecies, before up side then down
     * the order of the vertecies is clockwise
     */
    float[16] getVertecies(float xCenter, float yCenter);
}

#endif